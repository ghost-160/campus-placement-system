from django.db import models

# Note: We use Django's default User model with OneToOne Profile relationships
# StudentProfile (in students app)
# CompanyProfile (in companies app)
# This is cleaner than creating a custom User model after project initialization
